#include <unistd.h>


/**
 * 
 *              what define in unistd 
        文件 I/O：      read、write、close、lseek 等
        文件系统操作：   chdir、rmdir、mkdir、link、unlink 等  
        进程控制：      fork、exec、getpid、getppid、exit 等  
        用户和组：       getuid、getgid、geteuid、getegid 等   
        "环境：         getcwd、getlogin 等"  
        "其他：         sleep、usleep 等" }
 * 
*/


int main(){

    return 0;
}